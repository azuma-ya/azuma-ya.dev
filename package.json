{
  "name": "azuma-ya.dev",
  "version": "0.0.1",
  "devDependencies": {
    "@biomejs/biome": "^1.9.4",
    "prettier": "^3.4.2",
    "turbo": "^2.4.2",
    "typescript": "5.7.3"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "packageManager": "bun@1.1.37",
  "private": true,
  "scripts": {
    "build": "turbo build",
    "portfolio:build": "turbo build --filter=portfolio",
    "calendar:build": "turbo pages:build --filter=calendar",
    "editor:build": "turbo pages:build --filter=editor",
    "dev": "turbo dev",
    "check": "turbo check"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}
